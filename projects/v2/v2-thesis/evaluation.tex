\chapter{Results}
\label{ch:evaluation}

This chapter presents the results in seven sections: cohort description, model
performance and calibration, clinical utility (net benefit, operating points
and event-level detection), variance decomposition, treatment-anchored
evaluation, external validation, and equity analysis.

Every result below carries an explicit status tag --- \textbf{[C]}
confirmatory, \textbf{[E]} exploratory, \textbf{[D]} descriptive --- defined in
Section~\ref{sec:inference} and enumerated in full in
Table~\ref{tab:register}. Confirmatory results test a hypothesis whose
estimand, threshold and direction were fixed before any modelling; exploratory
results were pre-registered in scope only and generate hypotheses rather than
testing them. All intervals are 95\% percentile intervals from a stay-level
cluster bootstrap. The six pre-registered hypotheses are corrected by
Holm--Bonferroni at a family-wise error rate of 0.05; the exploratory subgroup
families are corrected by Benjamini--Hochberg at $q = 0.05$.

%===========================================================================


\section{Cohort Description and Label Variance \texorpdfstring{[D]}{[D]}}
\label{sec:cohort}
%===========================================================================

Table~\ref{tab:cohort} summarises the three cohorts. The base extraction
identifies 65,078 adult first ICU stays. The database and patients are the
same across all three variants; only the sepsis definition differs.

\begin{table}[H]
    \centering\small
    \begin{tabular}{llrrrrr}
        \toprule
        \textbf{Var} & \textbf{Name} & \textbf{Stays} & \textbf{Onsets}
        & \textbf{\%} & \textbf{Deaths} & \textbf{Onset h (IQR)} \\
        \midrule
        A & Narrow      & 65{,}078 & 6{,}252  & 9.6  & 5{,}934 & 10 [4--26] \\
        B & Seymour-Std & 65{,}078 & 6{,}447  & 9.9  & 5{,}892 & 11 [4--30] \\
        C & Liberal     & 65{,}078 & 12{,}581 & 19.3 & 4{,}956 & 10 [4--30] \\
        \bottomrule
    \end{tabular}
    \caption{Cohort description by label variant. The Liberal variant identifies
    about twice as many sepsis cases as the Narrow or Seymour-Standard variants,
        mainly because it accepts wider antibiotic-culture windows and uses a rolling
        SOFA baseline.}
    \label{tab:cohort}
\end{table}

The twofold difference in sepsis prevalence between Variant A (9.6\%) and
Variant C (19.3\%) confirms the label-sensitivity finding from Johnson et al.\
\cite{johnson2018comparative} at MIMIC-IV scale.

%===========================================================================


\section{Sample Size Adequacy \texorpdfstring{[C]}{[C]}}
\label{sec:sample_size_results}
%===========================================================================

\begin{table}[H]
    \centering\small
    \begin{tabular}{llrrrrr}
        \toprule
        \textbf{Var} & \textbf{Name} & \textbf{Train h} & \textbf{Events}
        & \textbf{EPH rate} & \textbf{Min-N} & \textbf{EPV} \\
        \midrule
        A & Narrow      & 2{,}304{,}923 & 4{,}647 & 0.00202 & 52{,}201 & 185.9 \\
        B & Seymour-Std & 2{,}301{,}842 & 4{,}847 & 0.00211 & 51{,}012 & 193.9 \\
        C & Liberal     & 2{,}120{,}376 & 9{,}403 & 0.00443 & 24{,}069 & 376.1 \\
        \bottomrule
    \end{tabular}
    \caption{Sample size results (Riley et al., 2019). EPV: events per candidate
    predictor variable (25 predictors). All three variants exceed the minimum
    required (52{,}946 stays each). Sample size is not the bottleneck.}
    \label{tab:sample_size}
\end{table}

%===========================================================================


\section{Model Performance: Internal Temporal Evaluation \texorpdfstring{[C]}{[C]}}
\label{sec:internal_performance}
%===========================================================================

Table~\ref{tab:internal_results} reports performance on the temporal test set
(2017--2019) under the unrestricted evaluation window.

\begin{table}[H]
    \centering\small
    \begin{tabular}{llrrrr}
        \toprule
        \textbf{Var} & \textbf{Model}
        & \textbf{AUROC} & \textbf{AUPRC}
        & \textbf{Utility} & \textbf{Alert burden} \\
        \midrule
        A & Primary & 0.760 & 0.0093 & $-1.000$  & $\infty$ \\
        A & GBT     & 0.742 & 0.0094 & $-4.667$  & 203      \\
        A & NEWS2   & 0.629 & 0.0030 & $-1.647$  & 221      \\
        A & Cox     & 0.642 & 0.0033 & $-10.056$ & 340      \\
        A & qSOFA   & 0.601 & 0.0027 & $-5.720$  & 344      \\
        A & SIRS    & 0.599 & 0.0029 & $-4.454$  & 299      \\
        \midrule
        B & Primary & 0.607 & 0.0030 & $-1.004$  & $\infty$ \\
        B & GBT     & 0.746 & 0.0092 & $-4.849$  & 194      \\
        B & NEWS2   & 0.635 & 0.0032 & $-1.616$  & 223      \\
        B & Cox     & 0.623 & 0.0031 & $-10.014$ & 359      \\
        B & qSOFA   & 0.609 & 0.0028 & $-5.503$  & 318      \\
        B & SIRS    & 0.600 & 0.0031 & $-4.332$  & 299      \\
        \midrule
        C & Primary & 0.759 & 0.0196 & $-1.000$  & $\infty$ \\
        C & GBT     & 0.752 & 0.0192 & $-2.475$  & 93       \\
        C & NEWS2   & 0.641 & 0.0072 & $-1.121$  & 97       \\
        C & Cox     & 0.661 & 0.0083 & $-4.406$  & 146      \\
        C & qSOFA   & 0.601 & 0.0060 & $-2.526$  & 151      \\
        C & SIRS    & 0.613 & 0.0068 & $-2.089$  & 128      \\
        \bottomrule
    \end{tabular}
    \caption{Internal temporal test performance (2017--2019, all variants use
    temporal split). N test rows: 548{,}854 (A/B), 504{,}828 (C). Utility:
    PhysioNet Utility Score; $\infty$ alert burden = no true alerts at default
    threshold. Benchmark: 1.4 (Moor et al., 2023). Confidence intervals for
    Variant B are in Table~\ref{tab:internal_ci}. \textbf{[C] confirmatory.}}
    \label{tab:internal_results}
\end{table}

\begin{table}[H]
    \centering\small
    \begin{tabular}{llll}
        \toprule
        \textbf{Model} & \textbf{AUROC} & \textbf{95\% CI} & \textbf{AUPRC (95\% CI)} \\
        \midrule
        Primary & 0.607 & 0.589--0.625 & 0.0030 (0.0027--0.0034) \\
        GBT     & 0.746 & 0.730--0.761 & 0.0093 (0.0080--0.0127) \\
        NEWS2   & 0.635 & 0.618--0.653 & 0.0032 (0.0029--0.0036) \\
        Cox     & 0.623 & 0.605--0.640 & 0.0031 (0.0028--0.0035) \\
        qSOFA   & 0.609 & 0.593--0.625 & 0.0030 (0.0026--0.0033) \\
        SIRS    & 0.600 & 0.584--0.617 & 0.0036 (0.0031--0.0042) \\
        \bottomrule
    \end{tabular}
    \caption{Variant B temporal test performance with 95\% stay-level
    cluster-bootstrap intervals ($B = 2{,}000$, 12{,}132 test stays,
    1{,}040 sepsis onsets). All six models are scored on the same replicates,
    so differences between rows may be read as paired contrasts. The GBT
    interval is disjoint from every other model's; the remaining five overlap
    substantially. \textbf{[C] confirmatory.}}
    \label{tab:internal_ci}
\end{table}

\subsection{Key Finding: Every Model Fails Clinically}

Every model under every label variant achieves a \textbf{negative} Utility
Score. This is the primary empirical finding. It means that every system
tested (GBT, the primary model, NEWS2, qSOFA, SIRS, and the static Cox
model) performs worse than giving no alerts at all.

Alert burdens range from 93 false alerts per true alert (GBT, Variant C) to
over 340 (Cox and qSOFA, Variants A/B), compared with the published benchmark
of 1.4 \cite{moor2023review}. The primary model issues no true alerts
at all ($\infty$ alert burden) under the default threshold.

This is consistent with Wang et al.\ \cite{wang2025srpm}: the
internal-to-external utility collapse they found across ninety-one models
already appears \textit{internally} on MIMIC-IV under an honest time-based
split. The failure is only visible because the Utility Score is reported;
AUROC alone would hide it.

Two qualifications apply, both developed in
Section~\ref{sec:clinical_utility}. First, these figures are computed at a
default 0.5 cut-off, which is not an operating point any deployment would
choose; at a threshold matched to the event prevalence the primary model does
achieve positive net benefit over both default strategies
(Table~\ref{tab:net_benefit}). Second, the Utility Score and net benefit
disagree here because they price false alarms differently, and that
disagreement is itself evidence for H5 rather than a reason to prefer one
number. The conclusion that survives both metrics is narrower than ``every
model fails'': no configuration tested reaches an alert burden a ward could
absorb, and only one of the six models can be tuned by threshold at all.

\subsection{Calibration \texorpdfstring{[C]}{[C]}}

Calibration intercept and slope are two summary numbers describing a whole
curve, so both the curve itself and a scalar summary of its distance from the
diagonal are reported. Following Van Calster et al.\
\cite{vancalster2019calibration}, the flexible calibration curve is a
restricted cubic spline of the outcome on the logit of predicted risk, and it
is summarised by the Integrated Calibration Index (ICI, the mean absolute
distance between predicted risk and that curve) and the scaled Brier score,
$1 - \text{Brier}/\text{Brier}_{\text{null}}$, where the null model predicts
the prevalence for every person-hour.

\begin{table}[H]
    \centering\small
    \begin{tabular}{llrrrrr}
        \toprule
        \textbf{Var} & \textbf{Model} & \textbf{Intercept} & \textbf{Slope}
        & \textbf{ICI} & \textbf{Brier} & \textbf{Scaled Brier} \\
        \midrule
        A & Primary & $-0.087$ & 1.029 & 0.00026 & 0.0018 & $+0.004$   \\
        A & GBT     & $-6.089$ & 1.265 & 0.400   & 0.179  & $-95.9$    \\
        A & Cox     & $-6.390$ & 1.233 & 0.509   & 0.265  & $-142.8$   \\
        \midrule
        B & Primary & $-0.393$ & 0.209 & 0.0027  & 0.0021 & $-0.129$   \\
        B & GBT     & $-6.064$ & 1.202 & 0.390   & 0.175  & $-91.6$    \\
        B & Cox     & $-6.376$ & 1.087 & 0.511   & 0.268  & $-140.5$   \\
        \midrule
        C & Primary & $-0.041$ & 1.097 & 0.00043 & 0.0042 & $+0.008$   \\
        C & GBT     & $-5.320$ & 1.882 & 0.435   & 0.203  & $-47.4$    \\
        C & Cox     & $-5.551$ & 1.351 & 0.502   & 0.262  & $-61.3$    \\
        \bottomrule
    \end{tabular}
    \caption{Calibration on the temporal test set. Perfect calibration is
    intercept = 0, slope = 1, ICI = 0. A scaled Brier score at or below zero
    means the model is no better than predicting the prevalence for everyone.
    NEWS2, qSOFA and SIRS are omitted: their score-to-probability lookups take
    only 18, 4 and 4 distinct values respectively, too few to support a
    flexible calibration curve.}
    \label{tab:calibration}
\end{table}

Two results stand out. First, only the primary model is calibrated at all.
Under Variants A and C its intercept is within 0.09 of zero, its slope within
0.10 of one, and its ICI is below 0.0005 --- roughly a quarter of the 0.2 per
cent event rate. Its scaled Brier score is positive, though barely
($+0.004$ and $+0.008$), meaning it improves on the null model by less than
one per cent.

Second, GBT and Cox are not merely imperfectly calibrated; they are
miscalibrated by roughly three orders of magnitude. Both carry intercepts near
$-6$, and their scaled Brier scores range from $-47$ to $-143$. A scaled Brier
of $-91.6$ means the GBT model's squared error is 92.6 times worse than simply
predicting the prevalence for every person-hour. Figure~\ref{fig:calibration}
shows why: GBT emits predicted risks between 0.2 and 0.9 for person-hours
whose observed event rate never exceeds 0.04. This matters directly for
Section~\ref{sec:clinical_utility}: a model whose probabilities are inflated
by two orders of magnitude cannot be operated at any threshold a clinician
would choose.

Variant B is the exception among the primary models, with a slope of 0.209 ---
the model over-spreads its predictions by roughly a factor of five. Its ICI
(0.0027) exceeds its own E90 (0.0016), which happens only when a small number
of extreme predictions dominate the mean; the Variant B model does in fact
emit predicted probabilities of exactly 1.0 for a handful of person-hours.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/11_calibration_curves.png}
    \caption{Flexible calibration curves on the temporal test set. Dashed line
    = perfect calibration; points = twenty equal-sized bins of predicted risk;
    shaded band = 95\% confidence interval. Each panel has its own scale
    because the predicted-risk ranges differ by orders of magnitude. The
    primary model tracks the diagonal under Variants A and C; GBT sits far to
    the right of it in every variant.}
    \label{fig:calibration}
\end{figure}

\subsubsection{A Correction to the Clamped Estimates}

The calibration intercepts and slopes above supersede those produced by the
metric-suite notebook, which computed them after clamping predicted
probabilities to $[0.001, 0.999]$. That guard is standard for models whose
predictions approach 0 or 1, but at a 0.2 per cent hourly event rate it is
actively harmful: 45\% (Variant A), 72\% (Variant B) and 7.5\% (Variant C) of
the primary model's predicted probabilities fall \emph{below} the clamp floor,
so the clamp was overwriting the majority of the quantity being measured. The
values in Table~\ref{tab:calibration} use a floor of $10^{-6}$, below the
smallest non-zero prediction in the test set. The correction moves the
Variant A slope from 1.169 to 1.029 and the Variant B slope from 0.318 to
0.209; the qualitative conclusions are unchanged, but the Variant A model is
better calibrated, and the Variant B model worse, than previously reported.
This is recorded here rather than silently patched, because it is exactly the
kind of scale-dependent analyst decision this study set out to measure.

%===========================================================================


\section{Clinical Utility: Net Benefit, Operating Points, and Event-Level
    Detection}
\label{sec:clinical_utility}
%===========================================================================

AUROC, AUPRC and the Utility Score describe a model; they do not describe what
a ward would experience if the model were switched on. This section reports
the four quantities that do: net benefit across the range of thresholds a
clinician might plausibly accept, the positive predictive value and false-alarm
rate at a fixed sensitivity, detection expressed per patient rather than per
person-hour, and the lead time gained when detection occurs.

One framing point governs everything below. Under the discrete-time
competing-risks specification, $\text{event}=1$ marks the sepsis onset hour
itself, so each septic stay contributes exactly one event hour out of a mean
of 45. Per-hour sensitivity therefore demands that the alert fire \emph{in the
onset hour}; per-patient sensitivity asks only whether the patient was alerted
at all before onset. The two are different questions, and reporting only the
first, as the metric suite in Section~\ref{sec:internal_performance} does,
understates detection while hiding the alarm volume that buys it.

\subsection{Decision-Curve Analysis \texorpdfstring{[C]}{[C]}}

Decision-curve analysis, pre-specified in Chapter~\ref{ch:methodology}, requires a choice of
threshold grid, and at this prevalence that choice determines the entire
result. A threshold probability of $t$ encodes a willingness to accept
$(1-t)/t$ false alarms per true detection. The hourly event rate here is
0.19--0.42 per cent, so the conventional grid of 0.01--0.20 sits between five
and one hundred times the prevalence: at $t=0.01$ the analyst is asserting a
tolerance of only 99 false alarms per detection, far below what an hourly
sepsis alert must absorb, and every curve falls trivially below ``treat
none''. Net benefit is therefore evaluated on a grid running from one tenth to
ten times the prevalence, following Vickers and Elkin \cite{vickers2006dca}.
The first run of the metric suite used the conventional grid and produced no
usable curve; that null result is a property of the grid, not of the models.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/11_decision_curves.png}
    \caption{Decision curves on the temporal test set. Net benefit against
    threshold probability, with both default strategies shown. The GBT curve
    lies exactly on top of ``treat all'' in every variant. Curves are clipped
    below $-1.75\times$ prevalence so the near-zero region stays legible; a
    line leaving the bottom of a panel is worse than treating nobody from that
    threshold onward.}
    \label{fig:decision_curves}
\end{figure}

\begin{table}[H]
    \centering\small
    \begin{tabular}{llrrrr}
        \toprule
        \textbf{Var} & \textbf{Model} & \textbf{Net benefit}
        & \textbf{Std NB} & \textbf{Alert rate} & \textbf{Alert-rate span} \\
        \midrule
        A & Primary & $7.03\times10^{-4}$ & 0.380 & 0.321 & 0.993 \\
        A & GBT     & $0$                 & 0.000 & 1.000 & 0.000 \\
        A & Cox     & $1.7\times10^{-8}$  & 0.000 & 1.000 & 0.000 \\
        A & NEWS2   & $1.45\times10^{-4}$ & 0.078 & 0.863 & 0.000 \\
        A & qSOFA   & $2.70\times10^{-4}$ & 0.146 & 0.656 & 0.000 \\
        A & SIRS    & $2.55\times10^{-4}$ & 0.138 & 0.590 & 0.000 \\
        \midrule
        B & Primary & $2.84\times10^{-4}$ & 0.150 & 0.179 & 0.649 \\
        B & GBT     & $0$                 & 0.000 & 1.000 & 0.000 \\
        B & Cox     & $3.5\times10^{-9}$  & 0.000 & 1.000 & 0.000 \\
        B & NEWS2   & $1.52\times10^{-4}$ & 0.080 & 0.862 & 0.000 \\
        B & qSOFA   & $2.84\times10^{-4}$ & 0.150 & 0.656 & 0.000 \\
        B & SIRS    & $2.74\times10^{-4}$ & 0.145 & 0.591 & 0.000 \\
        \midrule
        C & Primary & $1.68\times10^{-3}$ & 0.399 & 0.296 & 0.998 \\
        C & GBT     & $0$                 & 0.000 & 1.000 & 0.000 \\
        C & Cox     & $8.4\times10^{-9}$  & 0.000 & 1.000 & 0.000 \\
        C & NEWS2   & $3.54\times10^{-4}$ & 0.084 & 0.855 & 0.000 \\
        C & qSOFA   & $6.45\times10^{-4}$ & 0.153 & 0.644 & 0.000 \\
        C & SIRS    & $7.26\times10^{-4}$ & 0.172 & 0.579 & 0.000 \\
        \bottomrule
    \end{tabular}
    \caption{Net benefit at a threshold probability equal to the event
    prevalence. At that threshold both defaults have net benefit exactly zero
    and the maximum attainable net benefit is the prevalence itself, so
    standardised net benefit (Std NB = net benefit / prevalence) is the
    fraction of the attainable benefit captured. ``Alert-rate span'' is the
    change in alert rate across the whole threshold grid; a span of zero means
    the score cannot be tuned anywhere in the plausible range.}
    \label{tab:net_benefit}
\end{table}

Read on the correct threshold scale, decision-curve analysis gives the most
favourable result in this thesis --- and it is still a weak one. At a
threshold equal to the prevalence, the primary model captures 38.0\% (Variant
A) and 39.9\% (Variant C) of the attainable net benefit, and 15.0\% under
Variant B. This is genuine, if modest, value over both defaults, and it sits
awkwardly beside the uniformly negative Utility Scores in
Table~\ref{tab:internal_results}. The two metrics disagree because they price
errors differently: net benefit weights a false alarm by $t/(1-t)$, which at
$t=0.002$ is almost nothing, whereas the Utility Score imposes a fixed
$-0.05$ penalty per false-alert hour regardless of threshold. Which is right
is a clinical judgement, not a statistical one, and the divergence is itself
an instance of H5.

The alert-rate span column carries the more damaging finding. For every model
except the primary one it is exactly zero: across the entire plausible
threshold range, the alert rate does not move. GBT and Cox alert on
\emph{100\% of person-hours} at every threshold in this range, so their
decision curves are
not merely poor but identical to treating everyone --- the green GBT line in
Figure~\ref{fig:decision_curves} sits exactly on the treat-all reference.
This is the direct consequence of the calibration failure in
Table~\ref{tab:calibration}: with predicted probabilities inflated to the
0.2--0.9 range, no threshold below 4 per cent excludes anything. NEWS2, qSOFA
and SIRS are non-tunable for the opposite reason --- their integer scores map
to a handful of fixed probabilities, all above the grid --- and alert on a
constant 58--86 per cent of person-hours. The primary model is the only one of
the six that can be operated as a threshold-based alert at all.

\subsection{Operating Points at Fixed Sensitivity \texorpdfstring{[E]}{[E]}}

The default 0.5 cut-off used in Section~\ref{sec:internal_performance} is not
how an alerting system is commissioned; a sensitivity target is chosen and the
threshold follows. Table~\ref{tab:operating_points} sets the threshold to the
largest cut-off achieving 80 per cent per-hour sensitivity and reads off the
resulting operating characteristics.

\begin{table}[H]
    \centering\small
    \begin{tabular}{llrrrrrr}
        \toprule
        \textbf{Var} & \textbf{Model} & \textbf{Spec} & \textbf{PPV}
        & \textbf{NNE} & \textbf{Alerts/pt-day} & \textbf{FA/pt-day}
        & \textbf{FP:TP} \\
        \midrule
        A & Primary & 0.534 & 0.0032 & 315 & 11.19 & 11.16 & 314 \\
        A & GBT     & 0.530 & 0.0032 & 318 & 11.30 & 11.27 & 317 \\
        A & NEWS2   & 0.343 & 0.0023 & 439 & 15.78 & 15.75 & 438 \\
        A & Cox     & 0.364 & 0.0023 & 430 & 15.27 & 15.24 & 429 \\
        \midrule
        B & Primary & 0.293 & 0.0021 & 466 & 16.98 & 16.94 & 465 \\
        B & GBT     & 0.523 & 0.0032 & 315 & 11.46 & 11.42 & 314 \\
        B & NEWS2   & 0.343 & 0.0024 & 424 & 15.77 & 15.74 & 423 \\
        B & Cox     & 0.333 & 0.0023 & 440 & 16.02 & 15.99 & 439 \\
        \midrule
        C & Primary & 0.545 & 0.0074 & 135 & 10.96 & 10.87 & 134 \\
        C & GBT     & 0.529 & 0.0071 & 140 & 11.33 & 11.25 & 139 \\
        C & NEWS2   & 0.357 & 0.0054 & 187 & 15.44 & 15.36 & 186 \\
        C & Cox     & 0.395 & 0.0056 & 179 & 14.53 & 14.45 & 178 \\
        \bottomrule
    \end{tabular}
    \caption{Operating characteristics at a fixed per-hour sensitivity of
    80\%. NNE = number needed to evaluate ($1/\text{PPV}$), the alerted
    person-hours required per true event hour. FA/pt-day = false-alert hours
    per patient-day. qSOFA and SIRS are reported separately in the text
    because they degenerate at this target.}
    \label{tab:operating_points}
\end{table}

At 80 per cent sensitivity, PPV ranges from 0.0021 to 0.0074. The best case,
the primary model under the Liberal label, requires 135 alerted person-hours
per true event hour; the worst, the same model under the Seymour-Standard
label, requires 466. Every configuration generates between 10.9 and 17.0
false-alert hours per patient-day. A twelve-bed unit at the best operating
point would field roughly 130 false alert-hours per day.

Setting the threshold this way also exposes a limitation of the coarse
rule-based scores that the ratio-based alert burden concealed. SIRS cannot be
tuned to 80 per cent sensitivity under any variant, and neither can qSOFA
under the Liberal label: with only four distinct score values, the lowest
cut-off reaching the target is the one that alerts on every person-hour
(sensitivity 1.000, specificity 0.000, 23.9 false-alert hours per patient-day
--- an alert on every hour of every stay). qSOFA does reach the target under
Variants A and B, but only at specificity 0.344 and 15.7 false-alert hours per
patient-day. Their apparent competitiveness on AUROC in
Table~\ref{tab:internal_results} does not survive being asked to hit a
sensitivity target.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/11_false_alarms_vs_sensitivity.png}
    \caption{False alerts per patient-day against per-hour sensitivity, at
    sensitivity targets of 50\%, 80\% and 90\%. The relationship is close to
    linear across the whole range: there is no operating point at which
    sensitivity is obtained cheaply.}
    \label{fig:fa_vs_sens}
\end{figure}

\subsection{Per-Patient (Event-Level) Detection \texorpdfstring{[C]}{[C]}}

Table~\ref{tab:patient_level} re-expresses the same operating points per
patient. Episode sensitivity within the actionable window counts a septic stay
as detected if any alert fired between six hours before onset and onset
itself. A false-alarm episode is a contiguous run of alert hours that does not
overlap the window from six hours before to three hours after onset, so a
five-hour alert run counts once rather than five times.

\begin{table}[H]
    \centering\small
    \begin{tabular}{llrrrrr}
        \toprule
        \textbf{Var} & \textbf{Model}
        & \textbf{Ep.\ sens (6\,h)} & \textbf{Ep.\ sens (any pre)}
        & \textbf{Patient PPV} & \textbf{\% stays alerted}
        & \textbf{FA ep./pt-day} \\
        \midrule
        A & Primary & 0.859 & 1.000 & 0.084 & 100.0 & 0.55 \\
        A & GBT     & 0.911 & 0.996 & 0.085 & 98.5  & 1.26 \\
        A & NEWS2   & 0.954 & 0.980 & 0.084 & 97.6  & 2.14 \\
        A & Cox     & 0.869 & 0.904 & 0.088 & 85.6  & 0.62 \\
        \midrule
        B & Primary & 0.927 & 0.997 & 0.086 & 99.9  & 1.24 \\
        B & GBT     & 0.911 & 0.990 & 0.088 & 97.0  & 1.22 \\
        B & NEWS2   & 0.953 & 0.983 & 0.086 & 97.6  & 2.14 \\
        B & Cox     & 0.837 & 0.877 & 0.091 & 82.9  & 0.50 \\
        \midrule
        C & Primary & 0.850 & 1.000 & 0.175 & 100.0 & 0.57 \\
        C & GBT     & 0.896 & 0.995 & 0.176 & 98.9  & 0.88 \\
        C & NEWS2   & 0.952 & 0.982 & 0.177 & 97.4  & 2.14 \\
        C & Cox     & 0.866 & 0.911 & 0.189 & 84.5  & 0.53 \\
        \bottomrule
    \end{tabular}
    \caption{Per-patient detection at the 80\% per-hour sensitivity operating
    point, on 12{,}132 test stays of which 1{,}015 (A), 1{,}040 (B) and
    2{,}128 (C) are septic. Ep.\ sens (6\,h): fraction of septic stays alerted
    within $[\text{onset}-6\,\text{h},\ \text{onset}]$. Ep.\ sens (any pre):
    alerted at any point before onset. FA ep./pt-day: false-alarm episodes,
    not alert-hours, per patient-day.}
    \label{tab:patient_level}
\end{table}

Read alone, the first two columns look like a success: 85--95 per cent of
septic patients are alerted inside the actionable six-hour window, and
88--100 per cent are alerted at some point before onset. This is the number a
paper reporting only per-patient detection would headline.

The fifth column shows what it costs. To achieve it, the models alert on
82.9--100 per cent of \emph{all} stays. Patient-level PPV is consequently
0.084--0.091 under Variants A and B and 0.175--0.189 under Variant C --- which
are, to three decimal places, the septic fractions of the cohort themselves
(1{,}015/12{,}132 = 0.0837; 1{,}040/12{,}132 = 0.0857; 2{,}128/12{,}132 =
0.1754). At the patient level, being alerted carries no information about
whether a patient will become septic, because essentially every patient is
alerted. The primary model under Variants A and C alerts on 100.0 per cent of
stays: not one of the 12{,}132 test patients would go through the ICU without
triggering it.

\subsubsection{Per-Hour and Per-Patient, Side by Side}

\begin{table}[H]
    \centering\small
    \begin{tabular}{llrrrrrr}
        \toprule
        \textbf{Var} & \textbf{Model}
        & \textbf{Sens (hr)} & \textbf{Sens (pt)}
        & \textbf{PPV (hr)} & \textbf{PPV (pt)}
        & \textbf{FA hr/pt-day} & \textbf{FA ep./pt-day} \\
        \midrule
        A & Primary & 0.801 & 1.000 & 0.0032 & 0.084 & 11.16 & 0.55 \\
        A & GBT     & 0.801 & 0.996 & 0.0031 & 0.085 & 11.27 & 1.26 \\
        A & NEWS2   & 0.811 & 0.980 & 0.0023 & 0.084 & 15.75 & 2.14 \\
        \midrule
        B & Primary & 0.801 & 0.997 & 0.0021 & 0.086 & 16.94 & 1.24 \\
        B & GBT     & 0.801 & 0.990 & 0.0032 & 0.088 & 11.42 & 1.22 \\
        B & NEWS2   & 0.818 & 0.983 & 0.0024 & 0.086 & 15.74 & 2.14 \\
        \midrule
        C & Primary & 0.800 & 1.000 & 0.0074 & 0.175 & 10.87 & 0.57 \\
        C & GBT     & 0.800 & 0.995 & 0.0071 & 0.176 & 11.25 & 0.88 \\
        C & NEWS2   & 0.817 & 0.982 & 0.0054 & 0.177 & 15.36 & 2.14 \\
        \bottomrule
    \end{tabular}
    \caption{The same operating point viewed per person-hour and per patient.
    Both views are correct; they differ by a factor of roughly 25 in PPV and
    20 in alarm count. A study reporting only one column of each pair can
    make the same model look either useless or usable.}
    \label{tab:hour_vs_patient}
\end{table}

The gap between the two views is the methodological point of this section.
Per-hour PPV of 0.0032 and per-patient PPV of 0.084 describe one model at one
threshold on one test set. The choice of denominator moves the headline number
by a factor of twenty-six, which is larger than the spread this thesis
attributes to label definition (H1) or model class (H6). Denominator choice
belongs on the list of analyst decisions that require pre-specification.

\subsection{Lead Time to Onset \texorpdfstring{[C]}{[C]}}

Lead time is the interval between the first alert issued at or before onset
and the onset hour, over septic stays that were alerted at all.

\begin{table}[H]
    \centering\small
    \begin{tabular}{llrrrr}
        \toprule
        \textbf{Var} & \textbf{Model} & \textbf{Median (h)} & \textbf{IQR (h)}
        & \textbf{$\geq 3$\,h} & \textbf{$\geq 6$\,h} \\
        \midrule
        A & Primary & 8 & 4--20    & 0.895 & 0.638 \\
        A & GBT     & 8 & 3--19    & 0.839 & 0.600 \\
        A & NEWS2   & 8 & 4--20    & 0.861 & 0.611 \\
        A & Cox     & 7 & 4--19    & 0.785 & 0.557 \\
        \midrule
        B & Primary & 9 & 4--22    & 0.884 & 0.626 \\
        B & GBT     & 8 & 3--21.75 & 0.818 & 0.581 \\
        B & NEWS2   & 8 & 4--21    & 0.858 & 0.599 \\
        B & Cox     & 7 & 3--21    & 0.713 & 0.508 \\
        \midrule
        C & Primary & 7 & 3--21    & 0.842 & 0.588 \\
        C & GBT     & 7 & 3--21    & 0.835 & 0.584 \\
        C & NEWS2   & 8 & 4--21    & 0.859 & 0.599 \\
        C & Cox     & 7 & 4--21    & 0.781 & 0.547 \\
        \bottomrule
    \end{tabular}
    \caption{Lead time at the 80\% sensitivity operating point. The
    $\geq 3$\,h and $\geq 6$\,h columns are fractions of \emph{all} septic
    stays, not only detected ones.}
    \label{tab:lead_time}
\end{table}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/11_lead_time_distribution.png}
    \caption{Distribution of lead time before onset at the 80\% sensitivity
    operating point, truncated at 48 h. The mode sits in the first four hours
    with a long right tail.}
    \label{fig:lead_time}
\end{figure}

Median lead time is 7--9 hours and 51--64 per cent of septic stays are alerted
at least six hours ahead, which compares favourably with the 4--12 hour
horizons reported in the literature \cite{henry2015trewscore, moor2023review}.
These numbers must be read against the previous subsection. Lead time is
measured over stays that were alerted, and at these thresholds almost every
stay is alerted; an alarm that fires on every patient will, mechanically,
fire early on the septic ones. The right-skewed distribution in
Figure~\ref{fig:lead_time}, with a long tail past 24 hours, is a signature of
alerts unrelated to the impending onset. Lead time is a meaningful metric only
once the false-alarm rate is controlled, and it is not controlled here.

\subsection{External Clinical Utility on eICU-CRD \texorpdfstring{[C]}{[C]}}

\begin{table}[H]
    \centering\small
    \begin{tabular}{llrrrrr}
        \toprule
        \textbf{Var} & \textbf{Model} & \textbf{Spec} & \textbf{PPV}
        & \textbf{NNE} & \textbf{FA/pt-day} & \textbf{Patient PPV} \\
        \midrule
        A & Primary & 0.454 & $1.19\times10^{-5}$ & 84{,}213  & 13.10 & 0.00035 \\
        B & Primary & 0.137 & $7.51\times10^{-6}$ & 133{,}199 & 20.72 & 0.00033 \\
        C & Primary & 0.382 & $1.05\times10^{-5}$ & 95{,}335  & 14.83 & 0.00034 \\
        \midrule
        A--C & NEWS2 & 0.000 & $8.01\times10^{-6}$ & 124{,}889 & 24.00 & 0.00035 \\
        \bottomrule
    \end{tabular}
    \caption{eICU-CRD operating characteristics at the same 80\% target
    sensitivity, thresholds re-derived on the external cohort
    (7{,}868{,}005 person-hours, 181{,}580 stays, 63 event hours).}
    \label{tab:external_utility}
\end{table}

Externally the picture degrades by three orders of magnitude. PPV falls to
roughly $10^{-5}$, requiring 84{,}000 to 133{,}000 alerted person-hours per
true event hour, with 13--21 false-alert hours per patient-day. NEWS2 cannot
reach 80 per cent sensitivity on eICU without alerting on every person-hour
(specificity 0.000, 24 false-alert hours per patient-day). Patient-level PPV
of 0.00035 is again the cohort septic fraction (63/181{,}580 = 0.00035).

These external figures carry a substantial caveat. Only 63 event hours were
identified in 7.87 million eICU person-hours, an apparent sepsis incidence of
0.035 per cent of stays against 8.6 per cent in MIMIC-IV. A 250-fold
difference in apparent incidence is not plausible as a difference in patient
populations; it reflects incomplete recovery of the Sepsis-3 labelling inputs
in eICU, discussed in Section~\ref{sec:external}. The external operating
characteristics should therefore be read as an upper bound on how well the
models transfer, not as a measurement of it.

%===========================================================================


\section{Variance Decomposition and Hypothesis Verdicts \texorpdfstring{[C]}{[C]}}
\label{sec:decomposition}
%===========================================================================

\subsection{The Decomposition Table}

\begin{table}[H]
    \centering\small
    \begin{tabular}{p{4.6cm}cll}
        \toprule
        \textbf{Factor (others fixed)}         & \textbf{$\Delta$AUROC} & \textbf{95\% CI} & \textbf{H} \\
        \midrule
        Model class (label B, temporal)        & 0.145 & 0.133--0.165 & H6 \\
        Label definition (primary, temporal)   & 0.153 & 0.136--0.177 & H1 \\
        Split method (primary, label B)        & 0.123 & 0.100--0.147 & H4 \\
        Metric (AUROC vs Utility, int$\to$ext) & 0.170 & 0.102--0.242 & H5 \\
        \bottomrule
    \end{tabular}
    \caption{Variance decomposition with 95\% stay-level cluster-bootstrap
    intervals. All four factors produce AUROC changes of 0.12--0.17, and all
    four intervals overlap: no factor is separable from the others at this
    sample size. Most studies fix three of these factors and compare only
    models. \textbf{[C] confirmatory.}}
    \label{tab:decomposition}
\end{table}

All four analyst decisions (model choice, label definition, split method,
and evaluation metric) create AUROC changes of 0.12--0.17. Most studies fix
three of these and compare only models. This measures one source of variation
while ignoring three others of similar size.

The intervals sharpen rather than weaken this reading. Each individual factor
is estimated precisely enough to be clearly non-zero, so the claim that all
four decisions matter is secure. What the intervals do not support is any
ranking \emph{among} them: all four overlap, so the decomposition establishes
that the analyst's choices are collectively as consequential as the model
choice, not that any one of them dominates. H1 fails for exactly this reason.

\subsection{Corrected Confirmatory Verdicts}

\begin{table}[H]
    \centering\footnotesize
    \begin{tabular}{llrlrrl}
        \toprule
        \textbf{H} & \textbf{Contrast $\theta$} & \textbf{Est.} & \textbf{95\% CI}
        & \textbf{$p$} & \textbf{$p_{\text{Holm}}$} & \textbf{Verdict} \\
        \midrule
        H1 & Label spread $-$ model spread   & 0.007 & $-0.020$--$0.030$ & 0.310 & 0.931 & Not confirmed \\
        H2 & Unstable covariates (count)     & 18    & ---               & ---   & ---   & Descriptive \\
        H3 & Pre-treatment onsets (count)    & 0     & ---               & ---   & ---   & Structural \\
        H4 & Random $-$ temporal AUROC       & 0.123 & 0.100--0.147      & 0.0005 & 0.003 & Confirmed \\
        H5 & Internal $-$ external AUROC     & 0.170 & 0.102--0.242      & 0.0010 & 0.004 & Confirmed \\
        H6 & GBT $-$ primary AUROC           & 0.139 & 0.117--0.160      & 0.0005 & 0.003 & H6 rejected \\
        \bottomrule
    \end{tabular}
    \caption{The confirmatory family. One-sided cluster-bootstrap tests against
    each hypothesis's pre-registered threshold ($\theta_0 = 0$ for H1 and H5;
    0.02 for H4 and H6), Holm--Bonferroni corrected at family size $m = 6$ and
    FWER 0.05. H2 and H3 reserve a slot without contributing a p-value, which
    makes the correction on the other four stricter. The smallest reportable
    p-value is $1/(B+1)$. \textbf{[C] confirmatory.}}
    \label{tab:confirmatory}
\end{table}

Three of the four testable hypotheses survive Holm correction with adjusted
p-values of 0.003--0.004; the correction changes no verdict among them, because
the effects are large relative to their standard errors. The one verdict that
\emph{does} change is H1, and it changes on the uncorrected p-value already
(Section~\ref{sec:decomposition}). Multiplicity control is therefore not what
overturns H1 --- quantifying uncertainty at all is.

\subsection{H1: Label Dominance (Not Confirmed Under Correction)}

\begin{mdframed}[backgroundcolor=ATUOrange!20, linecolor=ATUOrange]
    \textbf{H1 [C]:} Spread in AUROC across label variants $\geq$ spread across
    model classes.

    \textbf{Point estimates:} label spread $= 0.153$ (95\% CI 0.136--0.177);
    model spread $= 0.145$ (95\% CI 0.133--0.165).

    \textbf{Contrast:} $\theta = 0.007$ (95\% CI $-0.020$--$0.030$),
    $p = 0.31$, Holm-adjusted $p = 0.93$.
    \textbf{H1 NOT CONFIRMED.}
\end{mdframed}

On point estimates alone H1 holds: changing the sepsis definition while keeping
the model fixed produces slightly more AUROC variation (0.153) than changing
the model while keeping the label fixed (0.145). Under the original protocol,
which specified no uncertainty quantification, this would have been recorded as
a confirmation.

It does not survive. The two spreads are estimated on the same test stays, so
their difference can be evaluated within bootstrap replicate; that difference
is 0.007 with an interval running from $-0.020$ to $+0.030$. The interval
covers zero comfortably, and the ordering reverses in roughly a third of
replicates. \textbf{The margin by which the label out-varies the model is
indistinguishable from sampling noise}, and this conclusion does not depend on
the multiplicity correction --- the uncorrected p-value is already 0.31.

What survives is the weaker but still substantive claim: the label spread and
the model spread are of \emph{comparable} magnitude, both around 0.15. That is
the finding relevant to Cohen et al.\ \cite{cohen2024subtle}. A literature that
fixes the label and compares only models is measuring one source of variance
while ignoring another that is at least its equal in size. Claiming the label
strictly dominates would require more data than this study has.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{images/09_label_variance_auroc.png}
    \caption{AUROC by label variant for each model (temporal split). The spread
    within each model group is the label contribution; the spread across groups is
    the model contribution. Both are of similar size.}
    \label{fig:label_variance_auroc}
\end{figure}

\subsection{H2: Coefficient Instability (Confirmed, Descriptive)}

\begin{mdframed}[backgroundcolor=ATUSand!30, linecolor=ATUGreen]
    \textbf{H2 [C]:} At least one feature changes sign or magnitude
    $\geq 2\times$ across label variants.

    \textbf{Result:} of the 20 covariates shared by all three variants, 10
    reverse sign, 17 change magnitude by $\geq 2\times$, and 18 do at least one
    of the two. \textbf{H2 CONFIRMED (descriptive; no test statistic).}

    \textbf{Multiplicity:} H2 reserves a slot in the confirmatory family of six
    but contributes no p-value, which makes the Holm correction on the
    remaining hypotheses stricter rather than weaker.
\end{mdframed}

H2 is a stability criterion, not a contrast between two estimable quantities,
so no p-value is defined for it. Quantifying the sampling variability of a
coefficient here would require either standard errors from the fitted
multinomial model, which the stored fit does not carry, or a bootstrap that
refits the model on 2.3 million training rows for each of 2,000 replicates,
which is not tractable. The instability is therefore reported as a count and
interpreted as descriptive evidence, and no inferential claim is attached to
it.

\begin{table}[H]
    \centering
    \begin{tabular}{lp{8cm}}
        \toprule
        \textbf{Feature}           & \textbf{What changed}                   \\
        \midrule
        MAP                        & Positive in Variant B, negative in A/C  \\
        Temperature                & Sign change across variants             \\
        Bilirubin                  & Sign change across variants             \\
        Platelets                  & Sign change across variants             \\
        Any vasopressor            & Sign change or magnitude $\geq 2\times$ \\
        Norepinephrine/epinephrine & Sign change across variants             \\
        Respiratory rate slope     & Sign change across variants             \\
        Platelets measured         & Magnitude change $\geq 2\times$         \\
        WBC measured               & Sign change or magnitude $\geq 2\times$ \\
        P/F ratio measured         & Sign change across variants             \\
        \bottomrule
    \end{tabular}
    \caption{Features with sign reversal or magnitude instability across label
    variants. Ten of the 20 shared covariates reverse sign; 18 reverse sign or
    change magnitude by at least twofold. Full per-covariate flags are in
    \texttt{12\_h2\_coefficient\_stability.csv}.}
    \label{tab:coef_instability}
\end{table}

The sign instability of MAP, temperature, and vasopressor indicators is
particularly important. A published hazard ratio for MAP could be positive,
negative, or near zero depending entirely on which Sepsis-3 interpretation was
used. Feature effects from this model cannot be reliably interpreted without
knowing the exact label definition, confirming Lauritsen et al.'s
\cite{lauritsen2021framing} framing-instability finding.

\subsection{H3: Treatment Leakage, Label Constitutionality (Confirmed)}

\begin{mdframed}[backgroundcolor=ATUPurple!10, linecolor=ATUPurple]
    \textbf{H3 [C]:} Restricting to pre-treatment hours reduces AUROC by
    $\geq 0.03$.

    \textbf{Result:} \textbf{0 sepsis onset events} in 179,003 pre-treatment
    person-hours. AUROC is undefined, so the pre-registered contrast cannot be
    formed. Exact one-sided 95\% upper bound on the per-hour onset rate:
    $1.7 \times 10^{-5}$, or fewer than 1 onset in 59,753 person-hours.
    \textbf{FINDING SUPERSEDES HYPOTHESIS.}

    \textbf{Multiplicity:} like H2, H3 reserves a slot in the confirmatory
    family without contributing a p-value.
\end{mdframed}

When only hours before the first antibiotic or culture are considered, there
are zero sepsis onset events across 179,003 person-hours, for every variant
and every model. This is not a small drop in performance. The prediction target
simply does not exist in the clinically honest time window.

Because the outcome is entirely absent, no discrimination metric and therefore
no test statistic can be computed, and it would be wrong to report the result
as a null finding. What can be stated exactly is how small the true rate must
be to be consistent with observing zero events: the one-sided 95\% upper bound
is $1.7 \times 10^{-5}$ per person-hour. Even at that upper bound, a 72-hour
ICU stay would carry at most a 0.12\% chance of a pre-treatment onset. The
absence is not a sampling artefact.

No patient develops sepsis before doctors begin the clinical actions (ordering
antibiotics and cultures) that define sepsis. This confirms Kamran et al.'s
\cite{kamran2024evaluation} argument at the structural level: the Sepsis-3
label on MIMIC-IV is \textbf{entirely constituted by clinical action}. Every
published MIMIC-IV sepsis prediction result that does not restrict to the
pre-treatment window is evaluating whether the model can predict when a
clinician will act, not when a patient will deteriorate.

\subsection{H4: Split Optimism (Confirmed)}

\begin{mdframed}[backgroundcolor=ATUSand!30, linecolor=ATUGreen]
    \textbf{H4 [C]:} Temporal-split AUROC is at least 0.02 lower than
    random-split AUROC.

    \textbf{Result:} Primary model: random AUROC = 0.730, temporal AUROC = 0.607.
    Difference: 0.123 (95\% CI 0.100--0.147). Test against the pre-registered
    threshold $\theta_0 = 0.02$: $p = 5.0\times10^{-4}$, Holm-adjusted
    $p = 0.003$. \textbf{H4 CONFIRMED.}
\end{mdframed}

\begin{table}[H]
    \centering
    \begin{tabular}{llrr}
        \toprule
        \textbf{Split} & \textbf{Model} & \textbf{AUROC} & \textbf{Utility Score} \\
        \midrule
        Temporal       & Primary        & 0.607          & $-1.004$               \\
        Random         & Primary        & 0.730          & $-1.000$               \\
        Temporal       & GBT            & 0.746          & $-4.849$               \\
        Random         & GBT            & 0.720          & $-5.167$               \\
        \bottomrule
    \end{tabular}
    \caption{Temporal vs.\ random split comparison (Variant B). The primary model
    gains 0.123 AUROC from the random split, far exceeding the 0.02 threshold.}
    \label{tab:split_optimism}
\end{table}

For the primary model, the random split inflates AUROC by \textbf{0.123}
compared to the temporal split, far above the 0.02 threshold. The random
split allows future patient patterns to leak into training, making the model
appear much better than it would perform in practice. This confirms Guo et
al.'s \cite{guo2022temporal} finding: studies that use random splits may
substantially overestimate real-world performance.

The GBT model shows a smaller, reversed pattern (temporal 0.746 vs.\ random
0.720), suggesting the tree ensemble is more robust to temporal shift than
the linear model. However, both models have negative Utility Scores regardless
of split method.

\subsection{H5: Metric Divergence (Confirmed)}

\begin{mdframed}[backgroundcolor=ATUSand!30, linecolor=ATUGreen]
    \textbf{H5 [C]:} Utility Score declines proportionally more than AUROC from
    internal to external validation.

    \textbf{Testable arm:} AUROC degradation internal $\to$ external (primary,
    Variant B) $= 0.170$ (95\% CI 0.102--0.242); internal 0.607, eICU-CRD 0.436
    (95\% CI 0.357--0.500). $p = 1.0\times10^{-3}$, Holm-adjusted $p = 0.004$.

    \textbf{Non-testable arm:} the internal Utility Score is already
    $-1.004$, so a proportional decline is undefined.
    \textbf{H5 CONFIRMED (qualitatively stronger).}
\end{mdframed}

Wang et al.'s \cite{wang2025srpm} finding, that AUROC understates
how badly models fail externally, is confirmed in a stronger form: the
Utility Score is already negative \textit{internally}. The ``crossing zero''
event that Wang et al.\ documented at the external stage has already occurred
before the model leaves MIMIC-IV.

The hypothesis as pre-registered compares two proportional declines, and only
one of them can be formed. A proportional decline is undefined for a quantity
that starts below zero, so the Utility Score arm is reported qualitatively and
the formal test is carried by the AUROC arm: external discrimination falls by
0.170, which is significantly greater than zero and also exceeds the
pre-registered external-degradation expectation of 0.085 from Moor et al.\
\cite{moor2023review}, whose value lies below the lower confidence bound. The
external AUROC interval (0.357--0.500) has an upper limit at chance, so the
model does not generalise to eICU-CRD in any usable sense.

\subsection{H6: Model-Class Null (Rejected)}

\begin{mdframed}[backgroundcolor=ATUOrange!20, linecolor=ATUOrange]
    \textbf{H6 [C]:} $|\Delta_\text{AUROC}(\text{primary} - \text{GBT})| \leq 0.02$.

    \textbf{Result:} $\Delta = 0.139$ (GBT: 0.746, primary: 0.607), 95\% CI
    0.117--0.160. Test against the equivalence bound $\theta_0 = 0.02$:
    $p = 5.0\times10^{-4}$, Holm-adjusted $p = 0.003$.
    \textbf{H6 REJECTED (non-equivalence established).}
\end{mdframed}

GBT outperforms the primary model by 0.139 AUROC under the Seymour-Standard
label. The entire confidence interval lies above the 0.02 equivalence bound, so
non-equivalence is established rather than merely suggested: the interpretable
model carries a substantial AUROC cost. Both models are scored on the same
person-hours, so the contrast is paired and the interval is correspondingly
tight.

But the ranking reverses on clinical utility: GBT has Utility Score $-4.849$
while the primary model has $-1.004$. The model with higher AUROC produces
more harmful false alerts. Which model is ``better'' depends entirely on
which metric is used.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{images/09_model_class_auroc.png}
    \caption{AUROC by model class (Variant B, temporal split). GBT substantially
    outperforms all other models by AUROC, but has the worst Utility Score. The
    model-class spread of 0.145 is comparable to the label spread of 0.153.}
    \label{fig:model_class_auroc}
\end{figure}

%===========================================================================


\section{External Validation on eICU-CRD \texorpdfstring{[C]}{[C]}}
\label{sec:external}
%===========================================================================

\begin{table}[H]
    \centering
    \begin{tabular}{llrrrr}
        \toprule
        \textbf{Var} & \textbf{Model} & \textbf{N rows} & \textbf{N events}
        & \textbf{AUROC (eICU)} & \textbf{AUROC (MIMIC)} \\
        \midrule
        A & Primary & 7{,}868{,}005 & 63 & 0.736 & 0.760 \\
        A & NEWS2   & 7{,}868{,}005 & 63 & 0.605 & 0.629 \\
        \midrule
        B & Primary & 7{,}868{,}005 & 63 & 0.437 & 0.607 \\
        B & NEWS2   & 7{,}868{,}005 & 63 & 0.605 & 0.635 \\
        \midrule
        C & Primary & 7{,}868{,}005 & 63 & 0.725 & 0.759 \\
        C & NEWS2   & 7{,}868{,}005 & 63 & 0.605 & 0.641 \\
        \bottomrule
    \end{tabular}
    \caption{External validation on eICU-CRD (frozen models, no retraining).
    Variants A and C show small drops (0.024 and 0.034). Variant B collapses by
    0.170 AUROC. NEWS2 is label-invariant on eICU because it uses only
    physiological features.}
    \label{tab:external}
\end{table}

External validation reveals a striking label-dependent pattern. Variants A and
C transfer well to eICU (drops of only 0.024 and 0.034 AUROC). Variant B.
the Seymour-Standard label, the most commonly used in the literature.
collapses by 0.170 AUROC.

NEWS2 is label-invariant (identical AUROC across all variants on eICU) because
it uses only physiological signals. Its small degradation (0.024) suggests that
the extra signal learned by the primary model under Variant B is
site-specific rather than physiologically general.

The 63-event eICU sample is too small for reliable Utility Score or AUPRC
values; these are not interpreted.

%===========================================================================


\section{Proportional-Hazards Testing \texorpdfstring{[C]}{[C]}}
\label{sec:schoenfeld}
%===========================================================================

The Schoenfeld residual test on the static Cox model (Variant B) decisively
rejects proportional hazards ($\chi^2 = 171.8$, df = 11,
$p = 5.0 \times 10^{-31}$). Five covariates violate the assumption
individually: temperature ($p = 2.4 \times 10^{-12}$), GCS
($p = 5.2 \times 10^{-19}$), creatinine ($p = 8.9 \times 10^{-5}$),
bilirubin ($p = 7.6 \times 10^{-6}$), and platelets ($p = 0.002$). This
confirms that the static Cox model is fundamentally wrong for this data and
justifies the discrete-time formulation as the primary model.

%===========================================================================


\section{GBT Feature Importance \texorpdfstring{[E]}{[E]}}
\label{sec:feature_importance}
%===========================================================================

\begin{table}[H]
    \centering
    \begin{tabular}{lrrr}
        \toprule
        \textbf{Feature}         & \textbf{Gain} & \textbf{Cover} & \textbf{Frequency} \\
        \midrule
        MAP slope (6\,h)         & 0.176         & 0.133          & 0.088              \\
        Temperature ($^\circ$C)  & 0.160         & 0.168          & 0.087              \\
        Resp.\ rate slope (6\,h) & 0.124         & 0.125          & 0.086              \\
        GCS                      & 0.091         & 0.084          & 0.068              \\
        WBC                      & 0.060         & 0.098          & 0.083              \\
        Creatinine               & 0.053         & 0.060          & 0.077              \\
        Bilirubin                & 0.050         & 0.058          & 0.071              \\
        Lactate                  & 0.048         & 0.054          & 0.065              \\
        MAP (level)              & 0.048         & 0.038          & 0.103              \\
        Platelets                & 0.041         & 0.045          & 0.079              \\
        \bottomrule
    \end{tabular}
    \caption{Top-10 GBT features by information gain (Variant B). Trend features
        (MAP slope, respiratory rate slope) are the most informative predictors. The
        direction of change over the past 6 hours carries more discriminative signal
        than the current value.}
    \label{tab:feature_importance}
\end{table}

The dominance of six-hour slope features is consistent with Lauritsen et al.'s
\cite{lauritsen2021framing} concern: the GBT model relies heavily on
\textit{trends} rather than \textit{levels}, meaning its performance depends
on the length of the history window used for feature construction.

%===========================================================================


\section{Equity Analysis \texorpdfstring{[E]}{[E]}}
\label{sec:equity}
%===========================================================================

\subsection{Performance Disparities by Subgroup \texorpdfstring{[E]}{[E]}}

\begin{table}[H]
    \centering\footnotesize\setlength{\tabcolsep}{3.5pt}
    \begin{tabular}{lrrcc}
        \toprule
        \textbf{Racial group} & \textbf{Rows} & \textbf{Sep}
        & \textbf{GBT AUROC (95\% CI)} & \textbf{Primary AUROC (95\% CI)} \\
        \midrule
        White               & 317{,}688 & 597 & 0.743 (0.722--0.764) & 0.614 (0.590--0.637) \\
        Unknown             & 107{,}158 & 235 & 0.773 (0.743--0.803) & 0.587 (0.547--0.628) \\
        Black/African Am.   & 31{,}716  & 58  & 0.743 (0.674--0.810) & 0.627 (0.542--0.706) \\
        Other               & 22{,}825  & 35  & 0.655 (0.557--0.743) & 0.561 (0.453--0.672) \\
        White -- Oth. Eur.  & 11{,}768  & 18  & 0.688 (0.574--0.794) & 0.664 (0.524--0.789) \\
        Asian               & 6{,}543   & 16  & 0.793 (0.675--0.891) & 0.628 (0.455--0.788) \\
        Hispanic/Latino (PR)& 6{,}462   & 16  & 0.641 (0.482--0.802) & 0.570 (0.411--0.735) \\
        Asian -- Chinese    & 5{,}386   & 13  & 0.583 (0.360--0.795) & 0.710 (0.567--0.826) \\
        \bottomrule
    \end{tabular}
    \caption{AUROC by racial subgroup (Variant B, temporal test) with 95\%
    stay-level cluster-bootstrap intervals, stratified within subgroup,
    $B = 2{,}000$. Interval width ranges from 0.042 (White, 597 onsets) to
    0.435 (Asian -- Chinese, 13 onsets). \textbf{[E] exploratory.}}
    \label{tab:equity_performance}
\end{table}

The GBT point estimates span 0.583--0.793, a spread of 0.210 that exceeds both
the model-class spread (0.145) and the label spread (0.153). Read without
intervals, that looks like the largest source of variance in the entire study.
The intervals show it is not: the two extreme groups carry 13 and 16 sepsis
onsets respectively, and their intervals are 0.435 and 0.216 wide. The apparent
spread is almost entirely a function of how few events the small subgroups
contain.

Table~\ref{tab:equity_contrasts} makes this explicit by contrasting each group
against the largest group (White) on the same bootstrap replicates.

\begin{table}[H]
    \centering\footnotesize
    \begin{tabular}{lrrcrr}
        \toprule
        \textbf{Racial group} & \textbf{Sep} & \textbf{Model}
        & \textbf{$\Delta$AUROC vs.\ White (95\% CI)} & \textbf{$p$} & \textbf{$q_{\text{BH}}$} \\
        \midrule
        Other                    & 35 & GBT     & $-0.088$ ($-0.188$--$0.003$) & 0.065 & 0.325 \\
        Unknown                  & 235& GBT     & $+0.031$ ($-0.006$--$0.067$) & 0.094 & 0.403 \\
        Asian -- Chinese         & 13 & GBT     & $-0.159$ ($-0.388$--$0.056$) & 0.143 & 0.476 \\
        Hispanic / Latino (PR)   & 16 & GBT     & $-0.102$ ($-0.262$--$0.062$) & 0.209 & 0.570 \\
        White -- Other European  & 18 & GBT     & $-0.055$ ($-0.171$--$0.051$) & 0.318 & 0.636 \\
        Asian                    & 16 & GBT     & $+0.051$ ($-0.069$--$0.152$) & 0.363 & 0.640 \\
        Black / African American & 58 & GBT     & $+0.001$ ($-0.072$--$0.071$) & 0.963 & 0.963 \\
        \midrule
        Asian -- Chinese         & 13 & Primary & $+0.096$ ($-0.049$--$0.215$) & 0.130 & 0.476 \\
        Unknown                  & 235& Primary & $-0.027$ ($-0.074$--$0.019$) & 0.270 & 0.619 \\
        Other                    & 35 & Primary & $-0.053$ ($-0.164$--$0.058$) & 0.362 & 0.640 \\
        White -- Other European  & 18 & Primary & $+0.050$ ($-0.097$--$0.181$) & 0.452 & 0.713 \\
        Hispanic / Latino (PR)   & 16 & Primary & $-0.044$ ($-0.203$--$0.121$) & 0.576 & 0.763 \\
        Black / African American & 58 & Primary & $+0.013$ ($-0.074$--$0.094$) & 0.768 & 0.874 \\
        Asian                    & 16 & Primary & $+0.014$ ($-0.160$--$0.174$) & 0.862 & 0.891 \\
        \bottomrule
    \end{tabular}
    \caption{Subgroup AUROC differences against the reference group (White, the
    largest group). $q_{\text{BH}}$ is the Benjamini--Hochberg adjusted p-value
    over exploratory family E1, which contains all 30 subgroup contrasts
    computed across race, language and insurance for both models --- not only
    the 14 shown here. \textbf{No racial contrast is significant}; the smallest
    adjusted value for race is $q = 0.325$. \textbf{[E] exploratory.}}
    \label{tab:equity_contrasts}
\end{table}

Only one contrast in the whole of family E1 survives correction, and it is not
a racial one: GBT discriminates better for Haitian-speaking patients than for
English-speaking patients ($\Delta = +0.149$, 95\% CI 0.041--0.232,
$q = 0.030$), on 12 sepsis onsets. On 12 events this is a hypothesis to test
elsewhere, not a finding.

The correct reading of Table~\ref{tab:equity_contrasts} is that this study is
\textbf{underpowered to detect subgroup disparities}, not that no disparities
exist. Several intervals are wide enough to be compatible with a disparity
larger than any effect reported anywhere else in this thesis: the GBT interval
for Asian -- Chinese patients runs from $-0.388$ to $+0.056$, so the data are
consistent with that group being served far worse than White patients, or
slightly better. Reporting the point estimates alone would have invited the
opposite conclusion --- that a spread of 0.210 constitutes evidence of
inequity, or that non-significance constitutes evidence of its absence. Neither
is supported.

Wang et al.'s \cite{wang2022equity} finding of degraded performance for Asian,
Hispanic and Spanish-speaking patients is therefore neither confirmed nor
contradicted here. The point estimates for Hispanic/Latino (Puerto Rican) and
Asian -- Chinese patients under GBT do fall below the White reference, in the
direction Wang et al.\ report, but the intervals cover zero comfortably.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.92\textwidth]{images/12_subgroup_auroc_ci.png}
    \caption{AUROC by racial subgroup with 95\% stay-level cluster-bootstrap
    intervals (Variant B, temporal test); sepsis onset count in parentheses.
    Interval width is governed almost entirely by the subgroup event count. The
    two groups with the most extreme point estimates are also the two least
    precisely estimated, and several intervals reach the 0.5 chance line.}
    \label{fig:auroc_by_race}
\end{figure}

\subsection{Label Sensitivity by Subgroup (Novel Finding) \texorpdfstring{[E]}{[E]}}

\begin{table}[H]
    \centering\footnotesize
    \begin{tabular}{lrcccc}
        \toprule
        \textbf{Racial group} & \textbf{Stays} & \textbf{A (\%)} & \textbf{B (\%)}
        & \textbf{C (\%)} & \textbf{Range (pp, 95\% CI)} \\
        \midrule
        White                    & 40{,}212 & 9.4  & 9.6  & 18.6 & 9.3 (8.9--9.7)   \\
        Unknown                  & 7{,}264  & 11.6 & 12.8 & 23.9 & 12.3 (11.3--13.3) \\
        Black / African American & 4{,}929  & 9.3  & 9.1  & 19.0 & 9.9 (9.0--11.1)  \\
        Other                    & 2{,}254  & 8.8  & 9.8  & 17.5 & 8.7 (7.1--10.2)  \\
        Unable to obtain         & 1{,}629  & 9.7  & 11.1 & 22.5 & 12.8 (10.7--14.9) \\
        White -- Other European  & 1{,}589  & 9.4  & 7.7  & 16.6 & 8.9 (7.2--10.7)  \\
        Asian                    & 799      & 10.5 & 11.5 & 21.3 & 10.8 (8.3--13.8) \\
        Asian -- Chinese         & 721      & 10.1 & 11.1 & 19.0 & 8.9 (6.4--11.9)  \\
        \bottomrule
    \end{tabular}
    \caption{Percentage of stays labelled as septic by racial subgroup and label
    variant, with a 95\% stratified cluster-bootstrap interval on the A--C
    range. The fraction roughly doubles from Variant A to C across all groups.
    Per-variant intervals are in \texttt{12\_subgroup\_labelsens\_ci.csv}.
    \textbf{[E] exploratory.}}
    \label{tab:equity_label}
\end{table}

The fraction of patients labelled as septic changes by 8.7--12.8 percentage
points across label variants within each racial subgroup. Unlike the
discrimination analysis, this contrast \emph{does} survive correction. Because
it is a stay-level proportion computed on the full 65,078-stay cohort rather
than a discrimination metric computed on the small number of onsets in the test
window, it is far better powered.

\begin{table}[H]
    \centering\footnotesize
    \begin{tabular}{lrcrr}
        \toprule
        \textbf{Racial group} & \textbf{Stays}
        & \textbf{$\Delta$ range vs.\ White (pp, 95\% CI)} & \textbf{$p$}
        & \textbf{$q_{\text{BH}}$} \\
        \midrule
        Unknown                  & 7{,}264 & $+3.01$ ($+1.93$--$+4.06$) & 0.001 & \textbf{0.006} \\
        Unable to obtain         & 1{,}629 & $+3.49$ ($+1.38$--$+5.55$) & 0.002 & \textbf{0.009} \\
        Black / African American & 4{,}929 & $+0.62$ ($-0.35$--$+1.87$) & 0.186 & 0.347 \\
        Asian                    & 799     & $+1.48$ ($-1.04$--$+4.50$) & 0.249 & 0.407 \\
        Other                    & 2{,}254 & $-0.63$ ($-2.26$--$+0.98$) & 0.425 & 0.510 \\
        White -- Other European  & 1{,}589 & $-0.41$ ($-2.10$--$+1.41$) & 0.635 & 0.714 \\
        Asian -- Chinese         & 721     & $-0.40$ ($-3.02$--$+2.61$) & 0.675 & 0.714 \\
        \bottomrule
    \end{tabular}
    \caption{Difference in label sensitivity (A--C range) against the reference
    group (White). $q_{\text{BH}}$ is adjusted over exploratory family E2, all
    18 label-sensitivity contrasts across race, language and insurance. Two
    racial contrasts survive at $q < 0.05$; across the full family, four do
    (adding Spanish-speaking patients, $+2.61$ pp, $q = 0.006$, and privately
    insured patients, $-1.76$ pp, $q = 0.006$). \textbf{[E] exploratory.}}
    \label{tab:equity_label_contrasts}
\end{table}

Two racial groups are significantly more label-sensitive than White patients
after false-discovery correction: Unknown ($+3.01$ pp, $q = 0.006$) and Unable
to obtain ($+3.49$ pp, $q = 0.009$). Both are groups whose race was not
recorded. Across the full family, Spanish-speaking patients are also more
label-sensitive than English-speaking patients ($+2.61$ pp, $q = 0.006$), and
privately insured patients are \emph{less} label-sensitive than
Medicare patients ($-1.76$ pp, $q = 0.006$).

The pattern is coherent: the groups whose sepsis status moves most when the
definition is loosened are those with the least complete documentation and the
weakest access to the care pathway that generates the culture and antibiotic
timestamps on which the label depends. Because the label is constituted by
clinical action (Section~\ref{sec:decomposition}), differential documentation
propagates directly into differential labelling --- and from there into who a
model is trained to identify.

This is the equity result that survives scrutiny in this study. The
discrimination disparity in Table~\ref{tab:equity_contrasts} does not: it is
too imprecisely estimated to support any claim. Reporting only the first
without the second would have inverted which of the two findings is defensible.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{images/10_label_sens_vs_auroc.png}
    \caption{Label sensitivity (fraction labelled septic) vs.\ AUROC by racial
    subgroup, across all three label variants. Horizontal spread = label
    sensitivity; vertical spread = performance variation. The two effects appear
    independent, but no association is tested: the vertical axis carries the
    uncertainty shown in Figure~\ref{fig:auroc_by_race}, which this plot does
    not display. \textbf{[E] exploratory, visual only.}}
    \label{fig:label_sens_vs_auroc}
\end{figure}

To the best of our knowledge, this is the first published report combining
subgroup model-performance disparities with subgroup label-sensitivity
variation for any sepsis model, following Wang et al.'s \cite{wang2022equity}
recommendation (see Section~\ref{sec:search_strategy} for the search strategy
supporting this claim). The combination is also what makes the two results
separable: only one of them is precise enough to act on.

%===========================================================================


\section{Analysis Register \texorpdfstring{[D]}{[D]}}
\label{sec:register}
%===========================================================================

Table~\ref{tab:register} records the status of every analysis reported in this
chapter and the multiplicity correction applied to it. It is generated by the
pipeline as \texttt{12\_analysis\_\allowbreak register\allowbreak .parquet}, not
maintained by hand, so it cannot drift from what the code actually computes.

\begin{table}[H]
    \centering\footnotesize\setlength{\tabcolsep}{4pt}
    \begin{tabular}{p{5.0cm}lp{1.9cm}p{4.2cm}}
        \toprule
        \textbf{Analysis} & \textbf{Status} & \textbf{Pre-reg.}
        & \textbf{Correction} \\
        \midrule
        Cohort description by label variant       & [D] & \S2     & none                                   \\
        Sample size adequacy                      & [C] & \S10    & none (single criterion)                \\
        Internal temporal performance             & [C] & \S5--6  & none (estimation)                      \\
        Calibration intercept and slope           & [C] & \S5     & none (estimation)                      \\
        Decision-curve net benefit                & [C] & \S6     & none (estimation)                      \\
        Operating points at fixed sensitivity     & [E] & ---     & none (descriptive)                     \\
        Per-patient detection and lead time       & [C] & \S6     & none (estimation)                      \\
        H1 label dominance                        & [C] & \S7     & Holm ($m=6$)                           \\
        H2 coefficient instability                & [C] & \S7     & slot reserved; no test statistic       \\
        H3 treatment leakage                      & [C] & \S7     & slot reserved; exact bound             \\
        H4 split optimism                         & [C] & \S7     & Holm ($m=6$)                           \\
        H5 metric divergence                      & [C] & \S7     & Holm ($m=6$)                           \\
        H6 model-class null                       & [C] & \S7     & Holm ($m=6$)                           \\
        External validation on eICU-CRD           & [C] & \S4     & none (estimation); feeds H5            \\
        Variance decomposition table              & [C] & \S1     & none (assembly of H1--H6)              \\
        Proportional-hazards testing              & [C] & \S8     & none (single check)                    \\
        GBT feature importance                    & [E] & ---     & none (ranking, no inference)           \\
        Subgroup AUROC by demographic group       & [E] & \S9$^*$ & Benjamini--Hochberg, family E1         \\
        Subgroup label sensitivity                & [E] & \S9$^*$ & Benjamini--Hochberg, family E2         \\
        Label sensitivity vs.\ AUROC association  & [E] & ---     & none (visual, no test)                 \\
        \bottomrule
    \end{tabular}
    \caption{Analysis register. [C] confirmatory, [E] exploratory,
    [D] descriptive. Section numbers refer to
    \texttt{00\_preregistration.md}. $^*$\S9 pre-registered the equity analysis
    in scope only: neither the subgroup levels, the reference level, nor the
    direction of any comparison was fixed in advance, so these analyses are
    exploratory.}
    \label{tab:register}
\end{table}

%===========================================================================


\section{Discussion}
\label{sec:discussion}
%===========================================================================

\subsection{The Core Finding}

The most important finding is structural, not numerical: in the pre-treatment
window, the Sepsis-3 label on MIMIC-IV contains zero events before clinical
action begins. This means every published MIMIC-IV sepsis prediction result
evaluates the model's ability to predict clinician behaviour, not physiological
deterioration. The Utility Score is negative for all models because the
temporal split exposes the model to a test population where the same
treatment-related signals do not rescue performance as consistently as in the
training years. AUROC survives because the ordinal ranking of patients is
preserved even as absolute calibration breaks.

\subsection{H1 and H6 Together}

H1 (label dominance) and H6 (model-class null) are in productive tension, as
designed, and the resolution is sharper than either hypothesis anticipated.
H6 is decisively rejected: the GBT gap is 0.139 with the whole interval above
the 0.02 equivalence bound. H1 is not confirmed: the margin by which the label
spread (0.153) exceeds the model spread (0.145) is 0.007, with an interval from
$-0.020$ to $+0.030$.

The two results together say something more precise than the pre-registration
allowed for. Both the label and the model contribute large variance --- around
0.15 AUROC each --- and this study cannot separate them. The conventional
literature, which fixes the label and reports model comparisons, is measuring
one source of variance while ignoring another of at least equal size. That
claim stands. The stronger claim, that the label matters \emph{more} than the
model, does not, and would need a study designed to detect a difference of
under 0.01 AUROC between two spreads.

This is also the clearest illustration in the thesis of why the inference layer
was necessary. On point estimates, H1 confirmed and the study told a tidy story
in which the label dominates. With intervals attached, that story is not
supportable, and the defensible finding is the more interesting one: two
sources of variance of comparable size, only one of which the field routinely
reports.

\subsection{Limitations}

\begin{enumerate}
    \item \textbf{Single-centre development.} All models were developed on
    MIMIC-IV, which originates from a single academic medical centre (Beth
    Israel Deaconess Medical Center, Boston). Clinical practices, patient
    demographics, and EHR documentation patterns at BIDMC may not represent
    those at other institutions. The equity and label-sensitivity findings in
    particular should not be assumed to transfer to hospitals with different
    patient populations or antibiotic prescribing cultures. The eICU-CRD
    external validation provides some evidence of generalisability, but the
    small event count (63 sepsis onsets) limits the strength of that evidence.

    \item \textbf{Small external event count.} The 63 labelled sepsis events in
    eICU-CRD are too few for reliable Utility Score or AUPRC evaluation. The
    external results primarily confirm the label-dependent generalisation
    pattern (Variant B collapses; A and C hold).

    \item \textbf{Deep survival model not implemented.} A deep survival
    comparator was planned but not built. This does not affect the structural
    findings about the label.

    \item \textbf{Limited feature set.} Clinical notes, procedure codes, and
    ventilator settings were not included. Richer features might improve
    discrimination but would not change the treatment-constitutionality finding.

    \item \textbf{Specific to Sepsis-3.} These findings apply to the Sepsis-3
    definition on MIMIC-IV. Other sepsis definitions or databases may behave
    differently.

    \item \textbf{Operating points are not externally recalibrated.} The
    thresholds in Section~\ref{sec:clinical_utility} are derived on the test
    set at which they are evaluated, so the reported PPV and false-alarm rates
    are optimistic: a prospectively fixed threshold would perform no better.
    The external operating points in Table~\ref{tab:external_utility} are
    likewise re-derived on eICU rather than transferred from MIMIC-IV, which
    isolates the discrimination loss but hides the additional loss a
    transferred threshold would incur.

    \item \textbf{Alarm episodes are defined by contiguity, not by
    acknowledgement.} False-alarm episodes are counted as contiguous runs of
    alert hours. A real system would apply suppression, snoozing and
    escalation logic, all of which would reduce the episode counts in
    Table~\ref{tab:patient_level}. The alert-hour rates are the conservative
    bound and the episode rates the optimistic one; the true burden lies
    between them.

    \item \textbf{The equity analysis is underpowered, not null.} The subgroup
    intervals in Table~\ref{tab:equity_performance} are wide enough that most
    groups cannot be distinguished from the reference group, and no contrast
    survives false-discovery correction. This is a statement about precision,
    not about fairness: the data are consistent both with no disparity and with
    disparities larger than any effect this thesis reports elsewhere. Detecting
    a subgroup AUROC gap of the size seen in the point estimates would require
    on the order of ten times the event count available in the smaller groups.
    The label-sensitivity result (Table~\ref{tab:equity_label}) is the better
    powered of the two equity analyses because it is a stay-level proportion
    rather than a discrimination metric, and does not depend on the small
    number of onsets in the test window.

    \item \textbf{The inference plan is a post-hoc amendment.} Confidence
    intervals and multiplicity control were added after the analyses had been
    run and inspected (Section~\ref{sec:inference}). They change no estimand,
    threshold or direction, but a reader should treat the corrected verdicts as
    the product of a pre-specified hypothesis set analysed under a
    retrospectively specified error-control plan, not as a fully pre-specified
    confirmatory design.

    \item \textbf{Two hypotheses admit no test statistic.} H2 is a stability
    criterion rather than a contrast, and the stored multinomial fit carries no
    variance estimate; H3 is degenerate because its outcome is entirely absent
    from the evaluation window. Both are reported descriptively while still
    consuming a slot in the family size, which makes the correction on the
    remaining four hypotheses stricter, not weaker.
\end{enumerate}
